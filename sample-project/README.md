# Items API

A simple REST API for managing items.

## Endpoints

- `GET /items` — list all items
- `GET /items/:id` — get a single item
- `POST /items` — create an item

## Notes

This is a sample project used for DevFlow AI demonstration purposes.
All data is synthetic and in-memory.
