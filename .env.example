# DevFlow AI — root environment variables
# Copy to .env and fill in values for local development.
# These values are only needed if you override defaults.

# Backend port (default: 3001)
# PORT=3001

# For Vercel deployment — URL of the deployed frontend to allow in CORS
# FRONTEND_URL=https://your-app.vercel.app
