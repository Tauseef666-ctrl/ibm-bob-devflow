# DevFlow AI frontend — Vite environment variables
# Copy to .env.local for local development overrides.

# If your backend is deployed separately (not on Vercel same-domain),
# set this to the backend URL. Leave empty for same-origin deployment.
# VITE_API_URL=https://your-backend.example.com
